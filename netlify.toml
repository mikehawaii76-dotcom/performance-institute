[build]
  functions = "netlify/functions"

# Run monthly report on the 1st of every month at 8am ET
[functions."monthly-report"]
  schedule = "0 13 1 * *"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
